#include <stdio.h>

int main() {
    int i = 0;
    //It prints the first value whether it satisfies the condition or not
    do
    {
        printf("%d\n", i);
        i++;
    } while (i < 10);
    
    return 0;
}